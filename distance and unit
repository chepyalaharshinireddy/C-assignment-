#include <stdio.h>

int main() {
    double speed, time;
    double distance;
    double meters, centimeters, millimeters;

    printf("Enter speed in km/h: ");
    scanf("%lf", &speed);

    printf("Enter time in hours: ");
    scanf("%lf", &time);

    // Calculate distance in kilometers
    distance = speed * time;

    // Convert kilometers into meters, centimeters and millimeters
    meters = distance * 1000;
    centimeters = distance * 100000;
    millimeters = distance * 1000000;

    // Display conversion results
    printf("\n--- Conversion Results ---\n");
    printf("Distance in Meters: %.2f m\n", meters);
    printf("Distance in Centimeters: %.2f cm\n", centimeters);
    printf("Distance in Millimeters: %.2f mm\n", millimeters);

    return 0;
}

Enter speed in km/h: 65.5
Enter time in hours: 1.5

--- Conversion Results ---
Distance in Meters: 98250.00 m
Distance in Centimeters: 9825000.00 cm
Distance in Millimeters: 98250000.00 mm
